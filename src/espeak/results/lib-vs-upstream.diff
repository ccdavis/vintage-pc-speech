--- upstream/src/libespeak-ng/dictionary.c	2026-09-19 23:08:04.269385316 -0500
+++ lib/dictionary.c	2026-09-20 01:54:29.740532475 -0500
@@ -41,7 +41,8 @@
 #include "compiledict.h"                   // for DecodeRule
 #include "synthdata.h"                     // for PhonemeCode, InterpretPhoneme
 #include "synthesize.h"                    // for STRESS_IS_PRIMARY, phoneme...
-#include "translate.h"                     // for Translator, utf8_in, LANGU...
+#include "translate.h"
+#include "../espk/espk.h"   /* ESPK: espk_data_get */                     // for Translator, utf8_in, LANGU...
 
 static int LookupFlags(Translator *tr, const char *word, unsigned int flags_out[2]);
 static void DollarRule(char *word[], char *word_start, int consumed, int group_length, char word_buf[N_WORD_BYTES], Translator *tr, int command, int *failed, int *add_points);
@@ -211,29 +212,23 @@
 	// Load a pronunciation data file into memory
 	// bytes 0-3:  offset to rules data
 	// bytes 4-7:  number of hash table entries
-	snprintf(fname, sizeof(fname), "%s%c%s_dict", path_home, PATHSEP, name);
-	size = GetFileLength(fname);
-
-	if (tr->data_dictlist != NULL) {
-		free(tr->data_dictlist);
-		tr->data_dictlist = NULL;
-	}
-
-	f = fopen(fname, "rb");
-	if ((f == NULL) || (size <= 0)) {
-		if (no_error == 0)
-			fprintf(stderr, "Can't read dictionary file: '%s'\n", fname);
-		if (f != NULL)
-			fclose(f);
-		return 1;
-	}
-
-	if ((tr->data_dictlist = malloc(size)) == NULL) {
-		fclose(f);
-		return 3;
+	snprintf(fname, sizeof(fname), "%s_dict", name);
+	{
+		long dlen = 0;
+		const void *d = espk_data_get(fname, &dlen);   /* ESPK: from the data pack */
+		size = (int)dlen;
+		if (tr->data_dictlist != NULL) {
+			if (!espk_data_owns(tr->data_dictlist))
+				free(tr->data_dictlist);
+			tr->data_dictlist = NULL;
+		}
+		if ((d == NULL) || (size <= 0)) {
+			if (no_error == 0)
+				fprintf(stderr, "Can't read dictionary file: '%s'\n", fname);
+			return 1;
+		}
+		tr->data_dictlist = (char *)d;   /* ESPK: used in place (read-only), no 182 KB copy */
 	}
-	size = fread(tr->data_dictlist, 1, size, f);
-	fclose(f);
 
 	pw = (int *)(tr->data_dictlist);
 	length = Reverse4Bytes(pw[1]);
--- upstream/src/libespeak-ng/synthdata.c	2026-09-19 23:08:04.269385316 -0500
+++ lib/synthdata.c	2026-09-19 23:23:39.350325967 -0500
@@ -40,7 +40,8 @@
 #include "soundicon.h"               // for soundicon_tab
 #include "synthesize.h"               // for PHONEME_LIST, frameref_t, PHONE...
 #include "translate.h"                // for Translator, LANGUAGE_OPTIONS
-#include "voice.h"                    // for ReadTonePoints, tone_points, voice
+#include "voice.h"
+#include "../espk/espk.h"   /* ESPK: espk_data_get */                    // for ReadTonePoints, tone_points, voice
 
 int n_tunes = 0;
 TUNE *tunes = NULL;
@@ -65,45 +66,18 @@
 
 static espeak_ng_STATUS ReadPhFile(void **ptr, const char *fname, int *size, espeak_ng_ERROR_CONTEXT *context)
 {
+	/* ESPK: the data files live in the packed ESPK.DAT (espk_data.c); the
+	 * pointers point straight into the pack, nothing is copied or freed. */
+	long length = 0;
+	const void *d;
+	(void)context;
 	if (!ptr) return EINVAL;
-
-	FILE *f_in;
-	int length;
-	char buf[N_PATH_BUF];
-
-	snprintf(buf, sizeof(buf), "%s%c%s", path_home, PATHSEP, fname);
-	length = GetFileLength(buf);
-	if (length < 0) // length == -errno
-		return create_file_error_context(context, -length, buf);
-
-	if ((f_in = fopen(buf, "rb")) == NULL)
-		return create_file_error_context(context, errno, buf);
-
-	if (*ptr != NULL) {
-		free(*ptr);
-		*ptr = NULL;
-	}
-	
-	if (length == 0) {
-		*ptr = NULL;
-		return 0;
-	}
-
-	if ((*ptr = malloc(length)) == NULL) {
-		fclose(f_in);
-		return ENOMEM;
-	}
-	if (fread(*ptr, 1, length, f_in) != length) {
-		int error = errno;
-		fclose(f_in);
-		free(*ptr);
-		*ptr = NULL;
-		return create_file_error_context(context, error, buf);
-	}
-
-	fclose(f_in);
+	d = espk_data_get(fname, &length);
+	if (d == NULL)
+		return ENS_NOT_INITIALIZED;
+	*ptr = (void *)d;
 	if (size != NULL)
-		*size = length;
+		*size = (int)length;
 	return ENS_OK;
 }
 
@@ -166,10 +140,7 @@
 
 void FreePhData(void)
 {
-	free(phoneme_tab_data);
-	free(phoneme_index);
-	free(phondata_ptr);
-	free(tunes);
+	/* ESPK: the data is part of the pack, not freed */
 	phoneme_tab_data = NULL;
 	phoneme_index = NULL;
 	phondata_ptr = NULL;
--- upstream/src/libespeak-ng/voices.c	2026-09-19 23:08:04.269385316 -0500
+++ lib/voices.c	2026-09-19 23:23:29.810326035 -0500
@@ -50,7 +50,13 @@
 #include "synthdata.h"                // for SelectPhonemeTableName, LookupP...
 #include "synthesize.h"               // for SetSpeed, SPEED_FACTORS, speed
 #include "translate.h"                // for LANGUAGE_OPTIONS, DeleteTranslator
-#include "wavegen.h"                  // for InitBreath
+#include "wavegen.h"
+/* ESPK: the voice files (en, !v/klatt) are embedded strings, see espk/voices_txt.c */
+#include "../espk/espk_vfile.h"
+#define fopen(name, mode) espk_vopen(name)
+#define fgets(buf, n, f) espk_vgets(buf, n, f)
+#define fclose(f) espk_vclose(f)
+#define GetFileLength(name) espk_vlength(name)                  // for InitBreath
 
 static int AddToVoicesList(const char *fname, int len_path_voices, int is_language_file);
 
--- upstream/src/libespeak-ng/translate.c	2026-09-19 23:08:04.269385316 -0500
+++ lib/translate.c	2026-09-20 01:54:29.740532475 -0500
@@ -113,11 +113,13 @@
 // other characters which break a word, but don't produce a pause
 static const unsigned short breaks[] = { '_', 0 };
 
+int espk_data_owns(const void *p);   /* ESPK: espk/espk_data.c */
+
 void DeleteTranslator(Translator *tr)
 {
 	if (!tr) return;
 
-	if (tr->data_dictlist != NULL)
+	if (tr->data_dictlist != NULL && !espk_data_owns(tr->data_dictlist))   /* ESPK: the dictionary is part of the data pack */
 		free(tr->data_dictlist);
 	free(tr);
 }
